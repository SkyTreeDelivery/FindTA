<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://www.mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.find.mapper.UserMapper">
    <sql id="user_columns">
        id, username, password, age, gender, face_image, face_image_big, nickname, email, gmt_created, gmt_modified, cid, phone, location, token
    </sql>

    <cache/>

    <select id="listUserByLoc" resultType="com.find.pojo.po.User">
        select
        <include refid="user_columns"/>
        from chat_user
        where ST_DWithin(location,#{centerPoint},#{redius})
        order by ST_Distance(location,#{centerPoint})
        limit #{userCount}
    </select>

</mapper>